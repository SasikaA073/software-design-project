spring.datasource.url=jdbc:mysql://localhost:3306/transformer_db
spring.datasource.username=admin
spring.datasource.password=admin
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File storage configuration (still used for temp handling if needed)
file.upload-dir=./uploads/

# AWS S3 (set real values via environment variables, do not commit secrets)
aws.s3.bucket=isiritransformer
aws.region=eu-north-1
# Expect AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY in environment

